import React from 'react';
import './Alert.css';

const Alert = (props) => {
	return (
		<div className="alert">
			
			{btnText && <button onClick={props.onDismiss}>{btnText}</button>}
			<button onClick={props.onDismiss}>Dismiss</button>
		</div>
	);
}

export default Alert;